import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.raimatak.speciesdb',
    appName: 'Fini Species Database',
    webDir: '../Frontend',


};

export default config;